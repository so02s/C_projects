#ifndef _ARITHMETIC
#define _ARITHMETIC

#include "../../_types.h"

int _add(_decimal value_1, _decimal value_2, _decimal *result);
int _sub(_decimal value_1, _decimal value_2, _decimal *result);
int _mul(_decimal value_1, _decimal value_2, _decimal *result);
int _div(_decimal value_1, _decimal value_2, _decimal *result);

#endif